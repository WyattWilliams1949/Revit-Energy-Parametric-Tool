<Window x:Class="RevitAddin.MatlabModeWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="MATLAB Mode Server" Height="250" Width="400"
        WindowStartupLocation="CenterScreen" ResizeMode="NoResize">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Text="MATLAB Mode Integration" FontSize="18" FontWeight="Bold" HorizontalAlignment="Center"/>
        
        <StackPanel Grid.Row="1" VerticalAlignment="Center" HorizontalAlignment="Center" Margin="0,20,0,0">
            <TextBlock x:Name="txtStatus" Text="Server Stopped." FontSize="14" HorizontalAlignment="Center" Foreground="Red" Margin="0,0,0,10"/>
            <TextBlock Text="Listening on: http://localhost:8080" FontSize="12" Foreground="Gray" HorizontalAlignment="Center"/>
        </StackPanel>
        
        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Center">
            <Button x:Name="btnStart" Content="Start Server" Width="100" Height="30" Margin="0,0,10,0" Click="BtnStart_Click"/>
            <Button x:Name="btnStop" Content="Stop Server" Width="100" Height="30" Margin="0,0,10,0" Click="BtnStop_Click" IsEnabled="False"/>
            <Button Content="Close" Width="100" Height="30" Click="BtnClose_Click"/>
        </StackPanel>
    </Grid>
</Window>
