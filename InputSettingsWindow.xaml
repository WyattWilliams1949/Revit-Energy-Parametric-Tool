<Window x:Class="RevitAddin.InputSettingsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Paste Saved Settings" Height="420" Width="620"
        WindowStartupLocation="CenterScreen" ResizeMode="CanMinimize">
    <Window.Resources>
        <Style TargetType="Button" x:Key="ActionBtn">
            <Setter Property="Height" Value="38"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="FontFamily" Value="Segoe UI"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" CornerRadius="6">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#3A6DB5"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="Paste Saved Settings" FontSize="20"
                   FontFamily="Segoe UI Light" Margin="0,0,0,6"/>

        <TextBlock Grid.Row="1" TextWrapping="Wrap" FontSize="12"
                   Margin="0,0,0,14"
                   Text="Copy the cipher string from the 'Copy for Input' cell in a previous Excel export and paste it below."/>

        <TextBox Grid.Row="2" x:Name="txtCipher" AcceptsReturn="False" TextWrapping="Wrap"
                 VerticalScrollBarVisibility="Auto"
                 FontFamily="Consolas" FontSize="11" Padding="10"
                 VerticalContentAlignment="Top"/>

        <TextBlock Grid.Row="3" x:Name="lblStatus" Foreground="#FF6B6B" FontSize="11"
                   Margin="0,6,0,10" TextWrapping="Wrap" Visibility="Collapsed"/>

        <Grid Grid.Row="4">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="12"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <Button Grid.Column="0" Content="&lt; Back" Width="90"
                    Style="{StaticResource ActionBtn}" Click="BtnBack_Click"/>

            <Button Grid.Column="2" Content="Review Inputs ›" Width="140"
                    Style="{StaticResource ActionBtn}" Click="BtnReview_Click"
                    ToolTip="Apply settings and open the setup window to review before running"/>

            <Button Grid.Column="4" Content="Don't Review and Run ›" Width="180"
                    Style="{StaticResource ActionBtn}" Click="BtnRunDirect_Click"
                    ToolTip="Apply settings and start the simulation immediately"/>
        </Grid>
    </Grid>
</Window>
